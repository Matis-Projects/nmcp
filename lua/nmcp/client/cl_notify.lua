net.Receive("NMCP::NOTIFICATIONS", function(len,ply)
	local string = net.ReadString()
	local type = net.ReadInt(4)
	local sound = net.ReadString()
	local time = net.ReadInt(7)
    notification.AddLegacy( string, type, time )
    surface.PlaySound( sound )
    Msg( string .. "\n" )
end)